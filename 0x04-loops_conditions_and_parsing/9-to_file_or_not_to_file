#!/usr/bin/env bash
# This script lists files and prints only the part after the first dash

for file in *; do
    # Skip hidden files automatically because * doesn't include .*
    before_dash="${file#*-}"
    echo "$before_dash"
done
